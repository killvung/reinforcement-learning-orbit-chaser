{
  "name": "orbit-chase",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "tsc -p tsconfig.test.json && node --experimental-specifier-resolution=node --test test/*.test.mjs",
    "test:parity": "tsc -p tsconfig.test.json && PYTHONPATH=rl .venv/bin/python rl/parity/compare.py"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "~6.3.1"
  }
}
